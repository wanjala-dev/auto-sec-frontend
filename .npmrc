# MUI v4 peers do not declare React 18 support; the app has always shipped
# with a legacy-resolved tree. Codify it so npm ci works on fresh checkouts
# (CI, EC2, new machines) exactly like the long-lived local node_modules.
legacy-peer-deps=true
